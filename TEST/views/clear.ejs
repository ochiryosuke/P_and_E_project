<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>SAMPLE</title>
  </head>
  <body>
      <ul>
        <h1>クリアしました</h1>
        <% clearstage.forEach((stage) => { %>
          <div>
            id：
            <span><%= stage.id %></span>
            Stage1：
            <span><%= stage.Stage1 %></span>
          </div>
        <% }) %>
        <a href="./reset"><p>クリア状況をリセットする</p></a>
      </ul>
  </body>
</html>